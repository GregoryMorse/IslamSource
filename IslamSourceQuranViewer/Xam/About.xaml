<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="IslamSourceQuranViewer.Xam.About">
    <ContentPage.Resources>
        <ResourceDictionary>
            <Style x:Key="TitleStyle" TargetType="Label">
                <Setter Property="TextColor" Value="Black"/>
                <!-- <Setter Property="FontFamily" Value="Global User Interface"/> -->
            </Style>
        </ResourceDictionary>
    </ContentPage.Resources>
    <Grid BackgroundColor="White">
        <ScrollView>
            <StackLayout Orientation="Vertical" BackgroundColor="White">
                <Label x:Name="DisplayName" HorizontalOptions="Center" FontSize="42" Style="{StaticResource TitleStyle}"></Label>
                <Label x:Name="Description" HorizontalOptions="Center" FontSize="28" Style="{StaticResource TitleStyle}"></Label>
                <Label x:Name="AboutInfo" FontSize="14" Style="{StaticResource TitleStyle}"></Label>
            </StackLayout>
        </ScrollView>
    </Grid>
    <ContentPage.ToolbarItems>
    </ContentPage.ToolbarItems>
</ContentPage>

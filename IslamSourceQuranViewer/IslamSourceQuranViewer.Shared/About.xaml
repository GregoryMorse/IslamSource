<Page
    x:Class="IslamSourceQuranViewer.About"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:IslamSourceQuranViewer"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <Page.Resources>
        <Style x:Key="TitleStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="FontFamily" Value="Global User Interface"/>
        </Style>
    </Page.Resources>
    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <ScrollViewer>
            <StackPanel Orientation="Vertical" Background="White">
                <TextBlock Name="DisplayName" HorizontalAlignment="Center" FontSize="42" Style="{StaticResource TitleStyle}"></TextBlock>
                <TextBlock Name="Description" HorizontalAlignment="Center" FontSize="28" Style="{StaticResource TitleStyle}" TextWrapping="Wrap"></TextBlock>
                <TextBlock x:Uid="AboutInfo" FontSize="14" Style="{StaticResource TitleStyle}" TextWrapping="Wrap"></TextBlock>
            </StackPanel>
        </ScrollViewer>
    </Grid>
    <Page.BottomAppBar>
        <CommandBar>
        </CommandBar>
    </Page.BottomAppBar>
</Page>
